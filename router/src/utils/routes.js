import Default from "../components/Default";
import Login from "../components/Login";

const routes = [
    {
        component: Login, 
        href: '/login'
    },
    {
        component: Default,
        href: '/'
    }
];

export default routes;