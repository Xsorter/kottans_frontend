import Default from "../components/Default";
import Login from "../components/Login";

const routes = [
    {
        href: '/login',
        component: Login
    },
    {
        href: '/',
        component: Default
    }
];

export default routes;